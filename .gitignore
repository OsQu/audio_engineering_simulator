/target
**/*.rs.bk

# Offline render outputs (Epic 2 harness). Committed golden references live elsewhere.
/renders

# wasm-pack build output (regenerated by `wasm-pack build`; see crates/wasm-bindings/README.md).
/crates/wasm-bindings/pkg

.DS_Store
