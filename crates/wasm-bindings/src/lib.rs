//! Browser/WASM bindings for the engine.
//!
//! Placeholder — the real `wasm32 + SIMD` build and JS bindings arrive in Epic 3
//! (Task 3.1.1). Kept in the workspace from day one so CI's `cargo check
//! --target wasm32-unknown-unknown` guards against non-portable code early.
