[package]
name = "wasm-bindings"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
publish.workspace = true

[lib]
# cdylib for the eventual WASM artifact; rlib so it stays unit-testable natively.
crate-type = ["cdylib", "rlib"]

[dependencies]
engine.workspace = true

[lints]
workspace = true
